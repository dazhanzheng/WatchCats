# 监督模式工作流程说明

## 更新日期
2025-08-13

## 概述
监督模式是 Baal 桌面宠物助手的核心功能之一，用于监控用户的电脑活动并在偏离设定目标时提供智能提醒。

## 工作流程

### 1. 检查频率
- **默认间隔**: 300秒（5分钟）
- **调试模式**: 5秒（通过环境变量 `SUPERVISION_CHECK_INTERVAL` 设置）
- **当前设置**: 5秒（用于调试）

### 2. 数据收集（每次检查时）

监督模式**同时**获取三个时段的活动数据：

```python
def _get_comprehensive_activity_stats():
    # 同时获取以下数据：
    stats_5m    # 过去5分钟 - 即时行为
    stats_2h    # 过去2小时 - 短期趋势  
    stats_24h   # 过去24小时 - 整体表现
    stats_today # 今日数据（从凌晨4点开始）
```

#### 数据内容示例
- **5分钟数据**: 当前正在使用的应用、活跃时长
- **2小时数据**: 各应用使用占比、切换频率
- **24小时数据**: 整体生产力统计、主要应用分布

### 3. AFK检测

在评估前先检查用户是否离开（AFK）：
- 如果用户持续AFK超过4分钟，跳过提醒
- 避免在用户不在电脑前时发送无效提醒

### 4. LLM智能评估

将所有时段数据发送给 LLM 进行综合分析：

```json
{
    "should_remind": true/false,        // 是否需要提醒
    "deviation_level": "严重/中度/轻微/无",  // 偏离程度
    "reminder_message": "提醒内容",      // 个性化提醒消息
    "analysis": "分析说明",              // 详细分析
    "time_period_analysis": {            // 各时段独立分析
        "5m": "5分钟行为分析",
        "2h": "2小时趋势分析",
        "24h": "24小时整体分析"
    }
}
```

### 5. 决策规则

LLM 根据以下规则进行决策：

1. **即时优先**: 如果5分钟数据显示用户正在做与目标相关的事，即使2小时或24小时有偏离，也不立即提醒
2. **趋势判断**: 如果5分钟和2小时都显示偏离，应该提醒
3. **鼓励改善**: 如果只是24小时整体偏离但当前正在改善，给予鼓励而非批评
4. **人设适配**: 提醒内容符合当前人设的语言风格
5. **避免打扰**: 只在明显偏离时才提醒，避免过度干扰

## 配置选项

### 监督目标设置
```python
# 长期目标
long_term_goal = "完成项目开发"

# 短期目标列表
short_term_goals = [
    "修复bug",
    "编写测试",
    "优化性能"
]
```

### 检查间隔调整
```bash
# 设置为5秒（调试用）
export SUPERVISION_CHECK_INTERVAL=5

# 恢复为300秒（生产环境）
export SUPERVISION_CHECK_INTERVAL=300

# 或使用脚本
./venv/bin/python set_supervision_interval.py 300
```

## 日志和调试

### 查看监督模式日志
1. **开发者控制台**: 右键托盘 -> 开发者模式 -> Supervision标签
2. **日志级别**: DEBUG模式下会显示详细的数据获取过程
3. **关键日志点**:
   - AFK检测结果
   - 各时段数据获取状态
   - LLM评估请求和响应
   - 最终决策结果

### 测试脚本
```bash
# 测试完整工作流程
./venv/bin/python test_supervision_workflow.py

# 测试5秒间隔监督
SUPERVISION_CHECK_INTERVAL=5 ./venv/bin/python test_supervision_5s.py

# 测试AFK检测
./venv/bin/python test_afk_detection_simple.py
```

## 性能优化

### 数据截断策略
- 每个时段数据限制在1000字符以内
- 保留最重要的应用信息（前N个应用）
- 避免发送过长的prompt给LLM

### 并发处理
- 同时获取多个时段的数据
- 使用上下文管理器确保连接复用
- 缓存LLM实例避免重复初始化

## 常见问题

### Q: 为什么有时看到5分钟数据，有时看到2小时数据？
**A**: 现在已修复，系统会同时获取所有时段数据并综合分析。

### Q: 如何调整提醒的敏感度？
**A**: 可以通过修改监督目标的具体程度来调整：
- 更具体的目标 = 更严格的监督
- 更宽泛的目标 = 更宽松的监督

### Q: 监督模式消耗多少资源？
**A**: 
- CPU: 极低（每次检查<1%）
- 内存: ~10MB
- 网络: 每次检查约2-3KB（LLM请求）

## 未来改进方向

1. **自适应间隔**: 根据用户行为模式动态调整检查频率
2. **历史趋势**: 保存和分析长期行为趋势
3. **智能静默**: 在特定时段（如会议）自动静默
4. **多目标支持**: 根据时间段切换不同的监督目标
5. **可视化报告**: 生成每日/每周生产力报告

---

*文档更新于 2025-08-13*